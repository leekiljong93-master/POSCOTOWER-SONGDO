# 패치 요약 — 포타송 설계서 작성 (Ver.260730)

Lv.3 → Lv.4 승격을 막고 있던 구조적 결함 ①~⑤를 일괄 정리했습니다.
자체 점검 스크립트 **34건 전부 통과**, 엑셀 산출물 생성 정상 확인.

---

## 파일 구성

| 파일 | 상태 | 역할 |
|---|---|---|
| `config.py` | **교체** | 구분 체계 단일 진실 소스, 스키마 상수, 로깅, 숫자 변환기 |
| `state_manager.py` | **신규** | 내역서 상태 단일 관리(SSOT) — 패치 ⑤ |
| `db_manager.py` | **교체** | 원자적 저장, 잠금, Result 타입, 오류 로깅 — 패치 ①②③ |
| `logic_calculator.py` | 부분 수정 | 원가 집계 정규화 + 미반영 행 감사(audit) — 패치 ④ |
| `ui_components.py` | **교체** | 하드코딩 제거, SSOT 경유, 충돌 팝업 추가 |
| `app.py` | **교체** | 위 계층 연결, 오류·경고 화면 노출 |
| `selftest_patch.py` | 신규 | 서버 없이 검증 (`python selftest_patch.py`) |

> `logic_calculator.py`의 엑셀 서식 로직(제목 블록·자동 열너비·A4 인쇄 설정)은 **원본 그대로 유지**했습니다. 손댄 부분은 상단 집계 함수뿐입니다.

---

## ① 원자적 저장 + 동시 저장 충돌 차단

**기존 문제** — `ws.clear()` 직후 `append_rows()`를 반복하는 구조여서, 중간에 쿼터 초과나 네트워크 오류가 나면 마스터 시트가 **비어 있는 상태로 남았습니다.** 두 사람이 동시에 저장하면 서로의 데이터를 지웠습니다.

**변경 사항**
- `_atomic_replace_worksheet()` 도입: 임시 시트에 전량 기록 → **행 수 검증** → 원본 이름 교체 → 구본 삭제. 실패 시 원본 이름을 되돌려 롤백합니다.
- 마스터 저장·백업 복구에 **권고적 쓰기 잠금**(`_쓰기잠금` 시트, TTL 180초) 적용. 다른 사용자가 저장 중이면 남은 대기 시간을 안내합니다.
- 프로젝트 저장에 **낙관적 동시성 검사**: 내가 불러온 시각과 시트의 저장 시각이 다르면 덮어쓰지 않고 `⚠️ 저장 충돌` 팝업으로 "덮어쓰기 / 최신본 확인"을 선택하게 합니다.
- 중단된 작업이 남긴 임시 시트는 6시간 후 자동 정리(`_cleanup_stale_sheets`).

> Google Sheets에는 원자적 CAS가 없어 완전한 상호배제는 아닙니다. "두 명이 같은 순간 저장"이라는 현실적 충돌을 차단하는 수준이며, 코드 주석에도 명시했습니다.

---

## ② 오류 침묵 제거

**기존 문제** — `except Exception: return pd.DataFrame(...)` 형태가 여러 곳에 있어, 인증 실패나 시트 오류가 화면에는 **"데이터 없음"으로 표시**됐습니다. 단가가 0원인 견적서가 조용히 만들어질 수 있는 구조였습니다.

**변경 사항**
- `config.setup_logging()` / `get_logger()`로 전역 로거 구성. 모든 예외를 `logger.exception()`으로 스택까지 기록합니다.
- `_get_master_items_raw()`가 `(DataFrame, 오류메시지)`를 반환하고, `consume_load_errors()`로 수집된 사유를 화면에 `⚠️ 데이터 조회 경고`로 표시합니다.
- 연결 실패 시 `init_db()`의 상세 사유를 화면에 노출합니다.
- 엑셀 생성 실패도 더 이상 조용히 넘어가지 않고 오류를 표시합니다.
- **부가 컬럼 유실 수정**: 읽을 때 `category_large` / `category_mid`를 버려서 재저장 시 사라지던 문제를 `MASTER_ALL_COLUMNS`로 보존하도록 고쳤습니다.

---

## ③ 반환 타입 통일

**기존 문제** — 같은 계층 함수가 `True` / 오류 문자열 / `{"status": ...}` dict를 섞어 반환했습니다. 호출부가 `if res is True`로 분기하다 보니 오류를 성공으로 오판할 여지가 있었습니다.

**변경 사항** — `Result` 데이터클래스로 일원화했습니다.

```python
res = db.save_project_to_cloud(name, df, expected_version=state.get_version(name))
if res:                      # __bool__ 지원
    st.success(res.message)
elif res.code == "conflict": # 충돌만 별도 처리
    ui.overwrite_conflict_dialog(name, res.data)
else:
    st.error(res.message)
```

- `code` 값으로 상황을 구분합니다: `conflict`, `locked`, `not_found`, `invalid`, `error`
- **하위 호환 유지**: 구버전 호출부의 `res["status"] == "success"`, `res["message"]` 접근도 그대로 동작합니다.

---

## ④ 구분 체계 일원화 (가장 위험했던 결함)

**기존 문제** — 마스터 시트는 `자재비/인건비/장비비/세트`, 내역서는 `자재/노무/장비`를 쓰면서 코드 곳곳에 문자열이 흩어져 있었습니다. 결과적으로:

- `df[df['구분'] == '자재비']` 정확 일치만 살아남아 **표기가 다른 행이 저장 시 조용히 삭제**
- `세트` 품목은 원가계산 필터에 없어 **금액이 0원으로 누락**
- 클라우드에서 복원된 `"14,400,000"` 같은 콤마 문자열 금액이 **0원으로 소멸**

**변경 사항**
- `config.SHEET_TO_GUBUN` / `GUBUN_TO_COST_GROUP` 매핑 테이블을 유일한 기준으로 삼고, `normalize_gubun()`·`normalize_sheet_name()`·`cost_group_of()`만 사용하도록 전 모듈을 정리했습니다. 이제 어떤 파일도 `'자재'`를 하드코딩하지 않습니다.
- `자재비`, `재료비`, `기계경비`, `Labor` 등 **이형 표기를 자동 흡수**합니다.
- `config.to_number_series()`로 콤마·통화기호를 제거한 뒤 숫자로 변환합니다.
- `calc.audit_categories()` 추가: 원가계산에 반영되지 못한 행과 금액을 찾아내 화면에 **🚫 빨간 오류**로 경고합니다. 금액 누락은 곧 견적 오류이므로 침묵시키지 않습니다.
- 마스터 저장 시 인식 불가한 구분 값이 있으면 **저장을 중단**하고 어떤 값이 몇 건인지 알려줍니다.

**검증 결과** (`selftest_patch.py`)

| 입력 | 구버전 결과 | 패치 후 |
|---|---|---|
| 자재 1,000,000 + 자재비 2,000,000 + 세트 500,000 | 1,000,000원 | **3,500,000원** |
| 노무 3,000,000 + 인건비 1,500,000 | 3,000,000원 | **4,500,000원** |
| 장비 `"800,000"` (문자열) | 0원 또는 TypeError | **800,000원** |

---

## ⑤ 이중 상태 관리 제거

**기존 문제** — `st.session_state.projects[name]`과 `st.session_state.estimate_data`가 서로를 `copy()`하며 동거했습니다. 한쪽만 갱신되는 순간 화면과 저장본이 어긋납니다.

**변경 사항** — `state_manager.py`가 단일 진실 소스를 관리합니다.

- **SSOT** = `projects[current_project]` 하나. `estimate_data`는 폐기했으며, 기존 세션에 남아 있으면 자동 흡수 후 삭제합니다.
- 읽기는 `state.get_estimate()`(사본 반환), 쓰기는 `state.set_estimate()` **한 곳뿐**입니다.
- 컬럼 정규화·`선택` 찌꺼기 제거·숫자 변환·`합계 = 단가 × 수량` 재계산이 모두 이 지점에서만 일어납니다. app.py에 흩어져 있던 방어 코드를 전부 걷어냈습니다.
- `has_changed()`는 dtype 차이(int64 vs float64)로 오판하지 않도록 값 기준으로 비교해, 불필요한 `st.rerun()` 반복을 막습니다.

---

## 적용 방법

1. 기존 파일을 백업합니다. (`git commit` 또는 폴더 복사)
2. 6개 파일을 덮어쓰고 `state_manager.py`를 추가합니다.
3. 검증: `python selftest_patch.py` → *34건 통과* 확인
4. 실행: `streamlit run app.py`

### 최초 실행 시 확인할 것

- 스프레드시트에 `_쓰기잠금` 시트가 자동 생성됩니다. **삭제하지 마세요.**
- 마스터 시트 헤더에 `category_large`, `category_mid`, `note` 열이 없으면 다음 저장 때 추가됩니다.
- 마스터 데이터의 `구분` 열에 `자재비/인건비/장비비/세트` 외의 값이 있으면 저장이 중단되고 해당 값을 알려줍니다. 안내대로 수정 후 다시 저장하세요.

### 로그 확인

```bash
PTS_LOG_LEVEL=DEBUG streamlit run app.py
```

---

## 남은 과제 (Lv.4 완성까지)

| 우선순위 | 항목 | 이유 |
|---|---|---|
| ⑥ | 편집 권한 통제 (비밀번호 또는 SSO) | 현재는 URL을 아는 누구나 마스터 단가를 바꿀 수 있습니다 |
| ⑦ | pytest 정식 도입 | 제비율 산식 변경 시 회귀 검증이 필요합니다 |
| ⑧ | 제비율 기준연도 관리 | 조달청 기준이 매년 바뀌므로 적용 연도를 산출물에 명시하는 편이 안전합니다 |

> ⚠️ 원가계산 결과는 적용 기준·연도에 따라 달라집니다. 계약·입찰 제출 전 최신 조달청 기준으로 재검토하시기 바랍니다. (이 문구를 README에만 두지 않고 원가계산 탭 화면에도 노출하도록 반영했습니다)
